# راهنمای سیستم ورود و ثبت‌نام کاربران

## 🎯 نمای کلی

سیستم احراز هویت کاربران نانومهر شامل صفحات ورود، ثبت‌نام و پروفایل کاربری است که به طور کامل به Supabase متصل است.

---

## ✨ امکانات

### 1. صفحه ثبت‌نام (Register)
- فرم ثبت‌نام کامل با فیلدهای:
  - نام و نام خانوادگی
  - ایمیل
  - شماره تماس (اختیاری)
  - رمز عبور
  - تأیید رمز عبور
- اعتبارسنجی داده‌های ورودی
- نمایش پیام‌های خطا و موفقیت
- ورود خودکار بعد از ثبت‌نام موفق
- طراحی responsive و زیبا

### 2. صفحه ورود (Login)
- فرم ورود ساده و کاربرپسند
- امکان نمایش/مخفی کردن رمز عبور
- ذخیره token و اطلاعات کاربر در localStorage
- مدیریت خطاها
- لینک به صفحه ثبت‌نام

### 3. پروفایل کاربری (Profile)
- نمایش اطلاعات کامل کاربر
- امکان ویرایش نام
- نمایش آمار حساب کاربری
- دکمه خروج از حساب
- طراحی مدرن با Glassmorphism

### 4. Header با قابلیت احراز هویت
- نمایش دکمه‌های "ورود" و "ثبت‌نام" برای کاربران مهمان
- نمایش نام کاربر و لینک به پروفایل برای کاربران وارد شده
- به‌روزرسانی خودکار بعد از ورود/خروج

---

## 🚀 نحوه استفاده

### برای کاربران:

#### 1. ثبت‌نام حساب جدید:

```
1. روی دکمه "ثبت‌نام" در Header کلیک کنید
2. فرم را پر کنید
3. روی "ثبت‌نام" کلیک کنید
4. منتظر بمانید تا حساب ایجاد شود
5. به طور خودکار وارد سیستم می‌شوید
```

#### 2. ورود به حساب:

```
1. روی دکمه "ورود" در Header کلیک کنید
2. ایمیل و رمز عبور را وارد کنید
3. روی "ورود به حساب کاربری" کلیک کنید
4. به صفحه اصلی منتقل می‌شوید
```

#### 3. مشاهده پروفایل:

```
1. بعد از ورود، روی نام کاربری خود در Header کلیک کنید
2. اطلاعات پروفایل نمایش داده می‌شود
3. می‌توانید نام خود را ویرایش کنید
```

#### 4. خروج از حساب:

```
1. وارد صفحه پروفایل شوید
2. روی دکمه "خروج از حساب" کلیک کنید
3. به صفحه اصلی منتقل می‌شوید
```

---

## 🔧 ساختار فنی

### فایل‌های ایجاد شده:

```
/components/
  ├── LoginPage.tsx       # صفحه ورود
  ├── RegisterPage.tsx    # صفحه ثبت‌نام
  └── UserProfile.tsx     # پروفایل کاربری
```

### Integration با Backend:

#### 1. ثبت‌نام:

```typescript
POST /auth/signup
Body: {
  email: string,
  password: string,
  name: string,
  phone?: string
}
```

#### 2. ورود:

```typescript
POST /auth/signin
Body: {
  email: string,
  password: string
}
Response: {
  access_token: string,
  user: Object
}
```

#### 3. اطلاعات کاربر:

```typescript
GET /auth/me
Headers: {
  Authorization: Bearer <token>
}
```

### LocalStorage:

سیستم از localStorage برای ذخیره اطلاعات استفاده می‌کند:

```javascript
// ذخیره token
localStorage.setItem('user_token', token);

// ذخیره اطلاعات کاربر
localStorage.setItem('user_data', JSON.stringify(user));

// خواندن token
const token = localStorage.getItem('user_token');

// خواندن اطلاعات کاربر
const userData = localStorage.getItem('user_data');
const user = JSON.parse(userData);

// پاک کردن (خروج)
localStorage.removeItem('user_token');
localStorage.removeItem('user_data');
```

---

## 🎨 طراحی UI

### رنگ‌ها:
- Primary: `#56CBD7` (آبی فیروزه‌ای)
- Secondary: `#45b9c5` (آبی تیره‌تر)
- Background: Gradient از `#0a1221` تا `#1a2737`
- Text: White & Gray tones

### ویژگی‌های طراحی:
- ✅ Gradient backgrounds
- ✅ Glassmorphism effects
- ✅ Smooth animations با Motion
- ✅ Responsive برای موبایل
- ✅ Icon-based UI با Lucide
- ✅ Form validation با feedback بصری

---

## 🔐 امنیت

### تدابیر امنیتی:

1. **Password Validation**:
   - حداقل 6 کاراکتر
   - تأیید رمز عبور

2. **Token Management**:
   - JWT tokens از Supabase
   - ذخیره امن در localStorage
   - انقضای خودکار

3. **Email Validation**:
   - بررسی فرمت ایمیل
   - ایمیل‌های یکتا

4. **Error Handling**:
   - نمایش پیام‌های خطای واضح
   - Logging در console
   - Fallback برای خطاهای شبکه

---

## 📱 Responsive Design

سیستم در تمام سایزهای صفحه به خوبی کار می‌کند:

### Desktop (> 1024px):
- فرم‌های مرکزی با max-width
- نمایش تمام عناصر Header
- Layout دو ستونه برای پروفایل

### Tablet (768px - 1024px):
- فرم‌های responsive
- Menu hamburger
- Layout تک ستونه

### Mobile (< 768px):
- فرم‌های تمام عرض
- Buttons استک شده
- نمایش بهینه روی صفحات کوچک

---

## 🧪 تست

### تست دستی:

1. **ثبت‌نام**:
   - [ ] ثبت‌نام با اطلاعات صحیح
   - [ ] خطا برای ایمیل تکراری
   - [ ] خطا برای رمز عبور ضعیف
   - [ ] خطا برای عدم تطابق رمز عبور

2. **ورود**:
   - [ ] ورود با اطلاعات صحیح
   - [ ] خطا برای ایمیل اشتباه
   - [ ] خطا برای رمز عبور اشتباه
   - [ ] نمایش/مخفی کردن رمز عبور

3. **پروفایل**:
   - [ ] نمایش اطلاعات کاربر
   - [ ] ویرایش نام
   - [ ] خروج از حساب
   - [ ] Redirect به login برای کاربران مهمان

4. **Header**:
   - [ ] نمایش "ورود/ثبت‌نام" برای مهمان‌ها
   - [ ] نمایش نام کاربر بعد از ورود
   - [ ] Update بعد از خروج

---

## 🔄 جریان کار کامل

```
1. کاربر جدید → ثبت‌نام → ورود خودکار → صفحه اصلی
2. کاربر قدیمی → ورود → صفحه اصلی
3. کاربر وارد شده → کلیک روی نام → پروفایل → ویرایش/خروج
4. کاربر مهمان → دکمه‌های ورود/ثبت‌نام در Header
```

---

## 🆕 امکانات آینده (اختیاری)

- [ ] فراموشی رمز عبور
- [ ] تأیید ایمیل
- [ ] ورود با Google/Facebook
- [ ] تغییر رمز عبور
- [ ] آپلود عکس پروفایل
- [ ] تنظیمات حریم خصوصی
- [ ] اعلان‌ها
- [ ] تاریخچه سفارشات

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. Console مرورگر را بررسی کنید
2. localStorage را چک کنید
3. Network requests را بررسی کنید
4. Backend logs را مشاهده کنید

---

**✅ سیستم ورود و ثبت‌نام آماده استفاده است!**

برای تست:
1. وب‌سایت را باز کنید
2. روی "ثبت‌نام" کلیک کنید
3. حساب جدید ایجاد کنید
4. از امکانات استفاده کنید!
