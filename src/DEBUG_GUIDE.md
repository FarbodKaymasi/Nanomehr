# 🔧 راهنمای Debug خطای Initialize

## مشکل:
```
Error initializing content: Error: خطا در مقداردهی اولیه
```

## مراحل Debug:

### مرحله 1: ورود به صفحه Debug
1. به آدرس زیر بروید:
   ```
   /#cms-debug
   ```
   یا در آدرس بار مرورگر تایپ کنید: `localhost:port/#cms-debug`

### مرحله 2: تست مرحله به مرحله
در صفحه Debug، دکمه‌های زیر را به ترتیب کلیک کنید:

#### 1️⃣ Test Login
- این دکمه را بزنید
- باید پیام "✅ Login موفق" ببینید
- Token باید ذخیره شود

#### 2️⃣ Test Auth  
- بعد از Login موفق، این دکمه را بزنید
- باید پیام "✅ احراز هویت موفق" ببینید
- Status باید 200 باشد

#### 3️⃣ Test Get Content
- این دکمه برای دریافت محتوای موجود است
- اگر محتوایی نیست، خطا نمی‌دهد فقط خالی است

#### 4️⃣ Test Initialize
- اگر مراحل 1 و 2 موفق بودند، این دکمه را بزنید
- باید پیام "✅ Initialize موفق" ببینید
- محتوای پیش‌فرض ایجاد می‌شود

### مرحله 3: بررسی Logs
در قسمت پایین صفحه، تمام logs نمایش داده می‌شود:
- Status code
- Response data
- پیام‌های خطا (اگر وجود داشته باشد)

## سناریوهای مختلف خطا:

### خطا 1: Status 401 (Unauthorized)
**علت:** Token معتبر نیست یا expire شده

**راه حل:**
1. دکمه "Test Login" را بزنید
2. Token جدید دریافت می‌شود
3. دوباره Test Initialize کنید

### خطا 2: Status 500 (Server Error)
**علت:** مشکل در server

**راه حل:**
1. Console مرورگر را باز کنید (F12)
2. به tab Network بروید
3. درخواست initialize را ببینید
4. Response را بررسی کنید

### خطا 3: "No Token"
**علت:** کاربر login نکرده

**راه حل:**
1. ابتدا Login کنید (دکمه 1️⃣)
2. سپس سایر تست‌ها را انجام دهید

## بررسی دستی Token:

در صفحه Debug، بخش "توکن‌های ذخیره شده" را بررسی کنید:
- **user_token:** اگر از سایت اصلی login کرده‌اید
- **cms_token:** اگر از پنل CMS login کرده‌اید

## اگر همه چیز موفق بود:

اگر تمام 4 تست با موفقیت انجام شد:
1. به `/#cms` بروید
2. وارد پنل CMS شوید
3. روی "مقداردهی اولیه" کلیک کنید
4. باید کار کند!

## دریافت کمک:

اگر همچنان مشکل دارید:
1. به `/#cms-debug` بروید
2. تمام 4 تست را انجام دهید
3. Screenshot از بخش Logs بگیرید
4. Console مرورگر (F12) را باز کنید
5. هر خطای قرمزی را کپی کنید
6. اطلاعات را ارسال کنید

---

**نکته مهم:** اگر کاربر Admin ایجاد نشده، ابتدا به `/#admin-setup` بروید و Admin را ایجاد کنید.
